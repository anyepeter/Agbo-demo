import React from 'react';
import PropTypes from 'prop-types';
import { Header } from '../../layout/header';
//import { Test } from './AboutUs.styles';

const AboutUs = (props) => (
  <>
    <Header />
  </>
);

AboutUs.propTypes = {
  // bla: PropTypes.string,
};

AboutUs.defaultProps = {
  // bla: 'test',
};

export default AboutUs;
